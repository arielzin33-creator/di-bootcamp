// Exercise 5 :

function weightConversion(weightGr) {
    return weightKg = weightGr / 1000
}
console.log(weightConversion(50))

const weightConversion2 = function(weightGr) {}
console.log(weightConversion2(50))

const weightConversion3 = (weightGr) => weightKg = weightGr / 1000
console.log(weightConversion3(50))

// The diffrence between function decleration to function expression, is that function expression will be assigned to a variable. and function decleration is not.